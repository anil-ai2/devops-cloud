### git 
* what is git and github. 
* Why do we need git/github 
* git commands
    + git init
    + git clone
    + git pull
    + git add 
    + git commit 
    + git push 
    + git branch 
    + git remote 

### github
* clone vs fork

### PR - Pull Request on GITHUB
